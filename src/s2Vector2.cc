#include "../include/s2Vector2.h"


namespace Spring2D
{
  // Canonical vectors
  const Vector2 Vector2::ZERO(0, 0);
  const Vector2 Vector2::X_VERSOR(1, 0);
  const Vector2 Vector2::Y_VERSOR(0, 1);
  const Vector2 Vector2::X_NEG_VERSOR(-1, 0);
  const Vector2 Vector2::Y_NEG_VERSOR(0, -1);


}
